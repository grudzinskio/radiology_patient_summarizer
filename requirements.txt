openai
python-dotenv
fastapi
textstat
rapidfuzz
pydantic
pandas
spacy>=3.8.5  # v3.8.5+ has pre-built wheels for Python 3.13 on Windows
scispacy  # Install with --no-deps to avoid downgrading spacy: pip install scispacy --no-deps
# After installing, download the spacy model:
# python -m spacy download en_core_sci_md